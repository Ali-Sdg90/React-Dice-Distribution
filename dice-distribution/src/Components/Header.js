import React from "react";

import Styles from "./Header.module.css";

const Header = () => {
    return (
        <div className={Styles.container}>
            <h1>Dice Distribution Showcase</h1>
        </div>
    );
};

export default Header;
