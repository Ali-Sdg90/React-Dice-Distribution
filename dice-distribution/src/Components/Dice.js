import React from 'react';

import Styles from "./Dice.module.css"

const Dice = () => {
    return (
        <div className={Styles.container}>
            
        </div>
    );
};

export default Dice;