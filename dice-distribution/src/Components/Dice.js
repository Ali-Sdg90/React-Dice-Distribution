import React from 'react';

import Styles from "./Dice.module.css"

const Dice = (props) => {
    return (
        <div className={Styles.container}>
            <div>HELLO</div>
        </div>
    );
};

export default Dice;